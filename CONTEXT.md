# 串通

串通是一个常驻的串口小三通：人和 Agent 同时看见、写入已被独占的串口设备。日常终端仍是人原来的软件，串通不是第二个串口助手。一份串通里可以有多条线路；每条线路独占一台设备。本机只跑一份串通。登录 Win11 后默认自己起来，托盘常驻。

对人说话时用「串通」：串口的串、三通的通。不是「勾结」那个意思。仓库和包名仍是 comtee。

## Language

**串通**:
本产品。一个常驻进程，持有零条或多条线路。本机只跑一份。登录 Win11 后默认自己起来，托盘常驻；自启可在面板里关掉。名字取串口的串、三通的通。
_Avoid_: 口通, 共线, 串口助手, 插件, 读屏, hub（对人说话时）

**设备**:
一根物理串口 gadget。用 USB 身份（VID:PID 与序列号）辨认，不用 COM 号辨认。COM 号只是此刻打开它的路径。
_Avoid_: COM 口, 端口号, 管子, 串口5（当它被当成身份时）

**线路**:
串通上的一条一对一配置：一个人端入口对应一台设备。人在面板上拆掉之前一直在；USB 拔掉不拆；串通退出再打开后从上次编排恢复。
_Avoid_: 会话, COM 映射, 端口对应串口（当它把 COM 号当成身份时）

**占口**:
线路存在则串通打开并独占该设备，与此刻有没有客户端无关。
_Avoid_: 有人连着才打开

**串口参数**:
线路占口时用的打开参数：波特率、数据位、校验、停止位、流控。默认 9600 8N1 无流控。人在面板上改，Agent 不能改。
_Avoid_: 只设波特率（当其余被当成不存在时）

**等待设备**:
线路还在、USB 不在。人端入口继续听，已连着的人端不断开；同一设备插回后按 USB 身份再占口，哪怕 COM 号变了。
_Avoid_: 线路没了, 拆线路（那是人在面板上做的事）

**解码**:
一条线路上，把设备字节变成 Agent 可读字的规则。默认 GBK。只是视图，不改管子里的字节。人在面板上改，Agent 不能改。
_Avoid_: 编码（当它听起来像改写了串口时）

**最近缓冲**:
Agent 进场时，可读视图里带的一段最近内容。不是面板日志，不是永久历史。
_Avoid_: 日志, 会话记录

**三通**:
一条线路上，设备与其客户端之间的共享关系：设备上来的字节每个客户端都有一份；任一客户端写下的字节都进设备。同一人端入口上可以挂多个 Telnet 客户端。
_Avoid_: 镜像, 读屏, 转发代理

**客户端**:
挂在某条线路上、参与该线路字节流的一端。人端和 Agent 端是客户端；面板不是。
_Avoid_: 用户, 连接（当其实是客户端时）

**人端入口**:
一条线路上给人连进来的本机 Telnet 端口。不同线路用不同端口。
_Avoid_: 2323（那是例子）, 全局端口, COM 口

**人端**:
任何连上人端入口的 Telnet 客户端。不绑定 MobaXterm；Moba 只是人日常用的那一种。
_Avoid_: Moba 插件, 串口会话（那是直接开 COM 的旧做法）

**Agent 端**:
Agent 连上串通的那一头。操作哪条线路由人当场告知 Agent；串通不替 Agent 猜设备。Agent 不得自己打开设备，也不得编排线路。可读字按该线路的解码来，进场时带最近缓冲。
_Avoid_: MCP（那是 Cursor 里的翻译层）, skill, 读屏

**面板**:
操作员用来编排线路、看占用冲突和谁连着的管理界面。面板不看、不写字节流。关掉面板不等于退出串通。只有人能在面板上创建、改、拆线路。
_Avoid_: 终端, 第二个串口助手, 实时日志

**人机共驾**:
人和 Agent 同时作为客户端挂在同一条线路上，两边随时可写，串通不设写锁。
_Avoid_: 读屏加模拟键盘, 抢焦点发键

**占用冲突**:
某条线路打不开它的设备（已被别人占用），或绑不上它的人端入口（该本机端口已被占用）。等待设备不是占用冲突。
_Avoid_: 错误（当其实是占用时）
